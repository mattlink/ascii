#!/usr/bin/env node

const ts = require('typescript');
console.log(ts);

const browserify = require('browserify');
console.log(browserify);



// used for building a game which uses this package
console.log('ascii-build coming soon');